<div class="header">
<center></center>
 <div class="container">
   
   <center><h1>Programmers Client List</h>
   <h2><%= link_to "Add New Project", '/projects/new' %>    <%=link_to"Project Page", '/projects' %>    <%= link_to "Main Menu", '/' %></h2></center>
 </div>
</div>

<div class="projectslist">
 <div class="container">
 
<% @progclient.each do |programmer, project| %>
<% pmfind = Programmer.find(programmer) %>
  	  <h3>Programmer: <%= programmer %>, <%= pmfind.name %><br></h3>
       <% @progclient.each do |project| %>
       <% clfind = Client.find(project.client_id) %>
         <li><%= clfind.name %><br></li>
       <% end %>
<% end %>
		
 </div>
</div>
